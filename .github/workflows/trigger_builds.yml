name: Trigger Yumemi builds

on:
  workflow_dispatch:

jobs:
  build_release:
    name: Trigger Yumemi builds 🚀
    uses: ./.github/workflows/build_release.yml

  build_legacy:
    name: Trigger Doki Legacy builds 🚀
    uses: ./.github/workflows/build_legacy.yml
